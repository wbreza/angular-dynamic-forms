article
    h1 {{schema.title}}
    h4 {{schema.description}}
    hr
    form(role="form", class="form-horizontal", ng-submit="form.onSubmit(dynamicForm)", novalidate)
        dynamic-editor(data-schema="schema", ng-model="model")
        div(class="form-group")
            div(class="col-sm-2")
            div(class="col-sm-10")
                section(ng-transclude)